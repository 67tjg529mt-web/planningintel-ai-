import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "PlanningIntel AI — Intelligent Planning Intelligence",
  description:
    "Automate the monitoring of English Local Planning Authorities. Detect updates to Local Plans, track Call for Sites, and get AI-powered insights for first-mover advantage.",
  keywords: [
    "planning intelligence",
    "local planning authorities",
    "call for sites",
    "local plan",
    "UK planning",
    "AI planning insights",
  ],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
      suppressHydrationWarning
    >
      <body className="min-h-full flex flex-col bg-white dark:bg-gray-950 text-gray-900 dark:text-gray-100">
        {children}
      </body>
    </html>
  );
}